apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "turbineproxy.fullname" . }}-config
  labels:
    {{- include "turbineproxy.labels" . | nindent 4 }}
data:
  turbineproxy.toml: |
    {{- .Values.config | nindent 4 }}
